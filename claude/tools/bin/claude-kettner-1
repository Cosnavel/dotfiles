#!/usr/bin/env bash
# Auto-generated launcher for Claude Code account: /Users/niclaskahlmeier/.claude-kettner-1
export CLAUDE_CONFIG_DIR="/Users/niclaskahlmeier/.claude-kettner-1"
for b in "$HOME/.local/bin/claude" "$HOME/.claude/local/claude" "/opt/homebrew/bin/claude" "/usr/local/bin/claude"; do
  [ -x "$b" ] && exec "$b" "$@"
done
exec claude "$@"
