We would now like to push the current changes to the current Git branch (do not switch branches; it must be the currently selected one). To do this, you should first test everything and check that everything is valid — run the `/test` and `/check` workflows, plus `/design` if the change touches UI. If errors occur there, you must of course check them before pushing. If you have created any temporary test files that are not part of the project, you must not commit them. For the commit, write a good commit message that contains all changes. Now validate, then commit, then push to the current branch!
